{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "msedge",
            "request": "launch",
            "name": "Launch Edge against localhost",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceFolder}",
            "preLaunchTask": "dev"
        },
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome/Brave against localhost",
            "preLaunchTask": "dev",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceFolder}"
        }
    ]
}